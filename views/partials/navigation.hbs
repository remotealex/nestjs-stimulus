<nav class="navigation __p __center">
    <h1>OpenConf</h1>
    <div>
        <a href="/" class="{{#if_eq current 'home'}}current{{/if_eq}}">Home</a>
        {{#if user}}
        <a href="/profile" class="{{#if_eq current 'profile'}}current{{/if_eq}}">Profile</a>
        <a href="/logout" data-turbolinks="false">Log out</a>
        {{else}}
        <a href="/login" class="{{#if_eq current 'login'}}current{{/if_eq}}">Log in</a>
        {{/if}}
    </div>
</nav>

<style>
    .navigation a {
        border-bottom: 2px solid transparent;
        display: inline-block;
        margin: 0 5px 40px;
        padding: 3px 9px;
    }

    .navigation a.current {
        border-bottom-color: #9b4dca;
    }
</style>